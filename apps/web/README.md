# ノートUIのSPA

`apps/server`を利用するSPA。

## 開発

```bash
# install dependencies
vp install

# run lint, fmt and type check
vp check --fix

# run tests
vp test

# run the development server
vp dev

# build the app
vp build
```
